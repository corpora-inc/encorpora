# Agent Notes (Third-Grade Homeschool Book)

- Blockquotes: one concept per blockquote; separate with a blank line (or text) between quotes. Avoid consecutive `>` definition lines because Pandoc merges them into one paragraph.
- If you need multiple definitions in a row, use regular bullets instead of stacking blockquotes.
