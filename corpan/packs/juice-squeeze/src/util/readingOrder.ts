/**
 * Reading-order flattening — MOVED to the cap-squeeze capability
 * (@shared/capabilities/squeeze). This shim keeps the pack's historical
 * import path pointing at the ONE implementation.
 */
export { flattenReadingOrder } from "@shared/capabilities/squeeze/src/readingOrder"
