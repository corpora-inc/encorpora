{
  "name": "corpan-stargate-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:corpan": "node scripts/dev-corpan.mjs",
    "dev:watch": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "pack": "node scripts/pack.mjs",
    "pack:all": "npm run build && node scripts/pack.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "^6.48.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.4.0",
    "vite": "^6.0.3"
  }
}
