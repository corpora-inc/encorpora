node_modules/
dist/
.vite/
.DS_Store
rasmapan.zip
.env
.env.local
.env.*.local
src/locales.generated.js

# Build artifacts — regenerated by build/extract_calliar_words.py
# (17 MB JSON) and build/audit_render.py (per-letter PNGs). Both
# scripts ARE tracked; their outputs aren't, to keep the repo lean.
build/vendor/calliar_recordings.json
build/render_audit/
