{
  "name": "corpan-rasmapan",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:locales && vite",
    "dev:corpan": "node scripts/dev-corpan.mjs",
    "dev:watch": "vite build --watch",
    "build": "npm run build:locales && vite build",
    "build:locales": "node scripts/build-locales.mjs",
    "preview": "vite preview",
    "pack": "node scripts/pack.mjs",
    "pack:all": "npm run build && node scripts/pack.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
