{
  "name": "encorpora",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n io,pages,games,watch,serve \"npm:dev:io\" \"npm:dev:pages\" \"npm:dev:games\" \"npm:dev:watch-games\" \"npm:dev:serve\"",
    "dev:io": "cd io && npm run dev",
    "dev:pages": "node pages/watch.js",
    "dev:games": "cd corpan/games/hover-runner && npm run dev:watch",
    "dev:watch-games": "node scripts/watch-games.js",
    "dev:serve": "wait-on io/.next/build-manifest.json && sleep 2 && node scripts/dev-server.js",
    "build": "npm run build:io && npm run build:pages && npm run build:games",
    "build:io": "cd io && npm run build",
    "build:pages": "node pages/build.js io/out",
    "build:games": "cd corpan/games/hover-runner && npm run build && cd ../../.. && npm run copy:games",
    "copy:games": "mkdir -p io/out/corpan/games/hover-runner && cp corpan/games/hover-runner/manifest.json io/out/corpan/games/hover-runner/ && cp -R corpan/games/hover-runner/dist/. io/out/corpan/games/hover-runner/",
    "serve": "cd io/out && python3 -m http.server 8000",
    "clean": "rm -rf io/out io/.next .local-pages"
  },
  "devDependencies": {
    "chokidar": "^4.0.3",
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
