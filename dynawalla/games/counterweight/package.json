{
  "name": "@dynawalla/game-counterweight",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "THE STEELYARD — the weigh-house of the Dynawalla Bazaar. The chit carries a column sum you must evaluate; the pan carries brass you steer with place-value weights. Put on the lightest brass that tips the beam and stamp the docket.",
  "engines": {
    "node": ">=22.12"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4327 --strictPort",
    "tsc": "tsc --noEmit",
    "test": "node --experimental-strip-types --no-warnings=ExperimentalWarning --test \"src/**/*.test.ts\"",
    "build:pack": "vite build --config vite.pack.config.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "typescript": "^6.0.0",
    "vite": "^8.1.5"
  }
}
