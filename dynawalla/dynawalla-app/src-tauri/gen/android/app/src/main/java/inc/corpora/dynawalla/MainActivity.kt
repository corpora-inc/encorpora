// Generated by `tauri android init` and TRACKED, unmodified.
//
// It is not tracked because it carries a decision — it is tracked because
// `tauri android init` REFUSES TO RUN without it. Once any part of gen/android
// is committed, a fresh checkout has a gen/android directory, and init then
// checks for the Java package directory and hard-errors:
//
//   Error Project directory .../gen/android/app/src/main/java/inc/corpora/
//   dynawalla does not exist. Did you update the package name in `Cargo.toml`
//   or the bundle identifier in `tauri.conf.json > identifier`?
//
// The message points at the bundle identifier, which is the wrong place to
// look and would cost an afternoon. Corpán's .gitignore allowlists its own
// MainActivity.kt for exactly this reason.
//
// So: do not delete this file, and if the package ever moves, move it with the
// identifier in the same commit.

package inc.corpora.dynawalla

import android.os.Bundle
import androidx.activity.enableEdgeToEdge

class MainActivity : TauriActivity() {
  override fun onCreate(savedInstanceState: Bundle?) {
    enableEdgeToEdge()
    super.onCreate(savedInstanceState)
  }
}
