name: Deploy Encorpora to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "web/io/**"
      - "corpan/games/**"
      - "web/pages/**"
      - "web/scripts/**"
      - ".github/workflows/hover-runner-pages.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ENCORPORA_BASE_PATH: /encorpora
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install io/ Dependencies
        working-directory: web/io
        run: npm install

      - name: Build io/ Site (Root)
        working-directory: web/io
        run: npm run build

      - name: Build Corpan Pages into io/out
        run: |
          node web/pages/build.js web/io/out

      - name: Install Hover Runner Dependencies
        working-directory: corpan/games/hover-runner
        run: npm install --legacy-peer-deps

      - name: Build Hover Runner
        working-directory: corpan/games/hover-runner
        run: npm run build

      - name: Package Hover Runner as ZIP
        run: |
          cd corpan/games/hover-runner
          zip -r hover-runner.zip manifest.json dist/
          cd ../../..

      - name: Package Hanzi Atelier as ZIP
        run: |
          cd corpan/games/hanzi-atelier
          zip -r hanzi-atelier.zip manifest.json index.js styles.css hanziwriter.min.js HANZIWRITER_LICENSE.txt data/
          cd ../../..

      - name: Copy Hover Runner into io/out
        run: |
          mkdir -p "web/io/out/corpan/games/hover-runner"
          cp corpan/games/hover-runner/manifest.json "web/io/out/corpan/games/hover-runner/"
          cp -R corpan/games/hover-runner/dist "web/io/out/corpan/games/hover-runner/"
          cp corpan/games/hover-runner/hover-runner.zip "web/io/out/corpan/games/hover-runner.zip"

      - name: Copy Hanzi Atelier into io/out
        run: |
          mkdir -p "web/io/out/corpan/games/hanzi-atelier"
          cp corpan/games/hanzi-atelier/manifest.json "web/io/out/corpan/games/hanzi-atelier/"
          cp corpan/games/hanzi-atelier/index.js "web/io/out/corpan/games/hanzi-atelier/"
          cp corpan/games/hanzi-atelier/styles.css "web/io/out/corpan/games/hanzi-atelier/"
          cp corpan/games/hanzi-atelier/hanziwriter.min.js "web/io/out/corpan/games/hanzi-atelier/"
          cp corpan/games/hanzi-atelier/HANZIWRITER_LICENSE.txt "web/io/out/corpan/games/hanzi-atelier/"
          cp -R corpan/games/hanzi-atelier/data "web/io/out/corpan/games/hanzi-atelier/"
          cp corpan/games/hanzi-atelier/hanzi-atelier.zip "web/io/out/corpan/games/hanzi-atelier.zip"

      - name: Show Final Structure
        run: |
          echo "Final site structure:"
          find web/io/out -type f | sort

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/io/out

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
